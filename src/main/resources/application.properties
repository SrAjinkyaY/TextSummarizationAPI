spring.application.name=Text Summarizer

# Google Gemini Configuration
spring.ai.google.genai.api-key=${GOOGLE_API_KEY}
spring.ai.google.genai.chat.options.model=gemini-2.5-flash
spring.ai.google.genai.chat.options.temperature=0.7

# Summarization Configuration
summarization.prompt.template=Please summarize the following text:\n\n{text}

# # Server port (change this to run the service on a specific port)
# server.port=8081

# # MySQL Database Configuration
# spring.datasource.url=jdbc:mysql://localhost:3306/springboodcrud
# spring.datasource.username=root
# spring.datasource.password=root
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # JPA/Hibernate Configuration
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# # Use JDBC-backed Spring Session (spring-session-jdbc is on the classpath)
# # Ensure Spring creates the required session tables automatically on startup
# # (useful for development or when the schema is not present).
# spring.session.store-type=jdbc
# spring.session.jdbc.initialize-schema=always